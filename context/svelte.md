### Initialize a new SvelteKit project

Source: https://svelte.dev/docs/svelte/getting-started/llms.txt

Commands to scaffold a new SvelteKit application, install dependencies, and start the development server. This is the official recommended approach for building Svelte applications.

```shell
npx sv create myapp
cd myapp
npm install
npm run dev
```

---

### Set Up Svelte with Vite

Source: https://svelte.dev/docs/svelte/getting-started

This demonstrates how to set up a Svelte project using Vite directly, which is an alternative to SvelteKit. It involves creating a new Vite project and selecting the Svelte option. The build command generates static assets.

```bash
npm create vite@latest
# Select the 'svelte' option
```

---

### ActionReturn Implementation Example

Source: https://svelte.dev/docs/svelte/svelte-action

Example showing how to return update and destroy methods along with custom attributes.

```typescript
interface Attributes {
	Attributes.newprop?: string | undefinednewprop?: string;
	'on:event': (e: CustomEvent<boolean>e: interface CustomEvent<T = any>
The **CustomEvent** interface can be used to attach custom data to an event generated by an application.


MDN Reference


CustomEvent<boolean>) => void;
}

export function function myAction(node: HTMLElement, parameter: Parameter): ActionReturn<Parameter, Attributes>myAction(node: HTMLElementnode: HTMLElement, parameter: Parameterparameter: type Parameter = /*unresolved*/ anyParameter): type ActionReturn = /*unresolved*/ anyActionReturn<type Parameter = /*unresolved*/ anyParameter, Attributes> {
	// ...
	return {
		update: (updatedParameter: any) => voidupdate: (updatedParameter: anyupdatedParameter) => {...},
		destroy: () => {...}
	};
}
```

---

### Install Vitest

Source: https://svelte.dev/docs/svelte/testing/llms.txt

Install Vitest as a development dependency for your project.

```sh
npm install -D vitest
```

---

### Mounting a component with target

Source: https://svelte.dev/docs/svelte/v5-migration-guide

Example of mounting a component to a specific DOM element.

```javascript
mount(App, { target: document.getElementById("app") });
```

---

### Initialize Parent and Child components

Source: https://svelte.dev/docs/svelte/context

Setup the component hierarchy where the Parent wraps the Child.

```svelte
<script>
	import Parent from './Parent.svelte';
	import Child from './Child.svelte';
</script>

<Parent>
	<Child />
</Parent>
```

```typescript
<script lang="ts">
	import Parent from './Parent.svelte';
	import Child from './Child.svelte';
</script>

<Parent>
	<Child />
</Parent>
```

---

### Writable Store with Start/Stop Notifiers

Source: https://svelte.dev/docs/svelte/stores

Demonstrates creating a writable store with optional `start` and `stop` notifier functions. The `start` function is called when the first subscriber appears, and `stop` is called when the last subscriber leaves.

```typescript
import { writable, type StartStopNotifier } from "svelte/store";

const customStore = writable<number>(0, (set, update) => {
  console.log("Store subscribed!");

  const interval = setInterval(() => {
    update((n) => n + 1);
  }, 1000);

  return () => {
    console.log("Store unsubscribed!");
    clearInterval(interval);
  };
});
```

---

### Install Vitest

Source: https://svelte.dev/docs/svelte/testing

Use npm to add Vitest as a development dependency.

```bash
npm install -D vitest
```

---

### Example: Sass Preprocessing

Source: https://svelte.dev/docs/svelte/v4-migration-guide

Illustrates using the `preprocess` function with a custom preprocessor group to convert Sass to CSS. The example shows defining markup and script preprocessor functions.

```typescript
const code: string;
const { code } = await preprocess(source, [
  {
    markup: () => {
      console.log("markup-1");
    },
    script: () => {
      console.log("script-1");
    },
  },
]);
```

---

### Hydrate usage example

Source: https://svelte.dev/docs/svelte/imperative-component-api

Demonstrates how to import and call the hydrate function with a target element and initial props.

```javascript
hydrate(App, {
  target: document.querySelector("#app"),
  props: { some: "property" },
});
```

---

### createSubscriber(start)

Source: https://svelte.dev/docs/svelte/svelte-reactivity

Returns a subscribe function that integrates external event-based systems with Svelte's reactivity. When called inside an effect, the provided start callback is invoked with an update function to trigger re-runs.

```APIDOC
## createSubscriber(start)

### Description
Returns a `subscribe` function that integrates external event-based systems with Svelte's reactivity. It is useful for integrating with web APIs like `MediaQuery`, `IntersectionObserver`, or `WebSocket`.

### Signature
`createSubscriber(start: (update: () => void) => (() => void) | void): () => void`

### Parameters
- **start** (function) - Required - A callback function that receives an `update` function. It should return an optional cleanup function that is called when effects are destroyed.

### Returns
- **subscribe** (function) - A function that, when called, registers the current effect to the reactive system.
```

---

### Component rendering and type definitions

Source: https://svelte.dev/docs/svelte/v5-migration-guide

Examples of importing components, rendering them to HTML/head, and the underlying render type definition.

```typescript
render } from 'svelte/server'; import
type App = SvelteComponent<Record<string, any>, any, any>
const App: LegacyComponentType
`App from './App.svelte'; const { html, head } = App.render({ props: { message: 'hello' }}); const { `const html: string`html, `const head: string`
HTML that goes into the <head>
head } =
render<SvelteComponent<Record<string, any>, any, any>, Record<string, any>>(component: ComponentType<SvelteComponent<Record<string, any>, any, any>>, options?: {
    props?: Omit<Record<string, any>, "$$slots" | "$$events"> | undefined;
    context?: Map<any, any>;
    idPrefix?: string;
    csp?: Csp;
    transformError?: ((error: unknown) => unknown | Promise<unknown>) | undefined;
} | undefined): RenderOutput
```

```typescript
render(`const App: LegacyComponentType`App, { `props?: Omit<Record<string, any>, "$$slots" | "$$events"> | undefined`props: { `message: string`message: 'hello' }});
```

---

### Svelte Await Block: Basic Syntax Examples

Source: https://svelte.dev/docs/svelte/await

Provides several concise examples of Svelte's {#await} block syntax, showcasing different ways to structure the await, then, and catch clauses for handling Promises.

```svelte
{#await expression}...{:then name}...{:catch name}...{/await}
```

```svelte
{#await expression}...{:then name}...{/await}
```

```svelte
{#await expression then name}...{/await}
```

```svelte
{#await expression catch name}...{/await}
```

---

### createSubscriber(start)

Source: https://svelte.dev/docs/svelte/svelte-reactivity

Creates a subscribe function that integrates external event-based systems with Svelte's reactivity. When called within an effect, the provided start callback receives an update function that triggers effect re-runs.

```APIDOC
## createSubscriber(start)

### Description
Returns a `subscribe` function that integrates external event-based systems with Svelte's reactivity. It is designed for use with web APIs like `MediaQuery`, `IntersectionObserver`, or `WebSocket`.

### Signature
`createSubscriber(start: (update: () => void) => (() => void) | void): () => void`

### Parameters
- **start** (function) - Required - A callback function that receives an `update` function. Calling `update` triggers any effects that have called the returned `subscribe` function to re-run. This callback may optionally return a cleanup function to be executed when effects are destroyed.

### Usage
When `subscribe` is called inside an effect, the `start` callback is invoked. If multiple effects call `subscribe`, `start` is only called once, and the cleanup function is only executed when all associated effects are destroyed.
```

---

### Derived state example

Source: https://svelte.dev/docs/svelte/svelte

Example of using $derived with an asynchronous data fetch.

```javascript
const data = $derived(await getData(id));
```

---

### Install jsdom for Component Testing

Source: https://svelte.dev/docs/svelte/testing/llms.txt

Install jsdom as a development dependency to provide DOM APIs for component testing.

```sh
npm install -D jsdom
```

---

### Derived State Reactivity Example

Source: https://svelte.dev/docs/svelte/runtime-warnings

Example of how Svelte tracks state within a derived expression containing an await.

````typescript
let let total: numbertotal = ```
function $derived<number>(expression: number): number
namespace $derived
````

`Declares derived state, i.e. one that depends on other state variables. The expression inside`$derived(...)` should be free of side-effects.
Example:

```
let double = $derived(count * 2);
```

@see
{@link https://svelte.dev/docs/svelte/$derived Documentation}
@param
expression The derived state expression
$derived(await `let a: Promise<number>`a + `let b: number`b);

````

--------------------------------

### Write a Vitest unit test

Source: https://svelte.dev/docs/svelte/testing

Example of testing a Svelte-based utility function using Vitest.

```javascript
import { flushSync } from 'svelte';
import { expect, test } from 'vitest';
import { multiplier } from './multiplier.svelte.js';

test('Multiplier', () => {
	let double = multiplier(0, 2);

	expect(double.value).toEqual(0);

	double.set(5);

	expect(double.value).toEqual(10);
});
````

---

### Test Svelte components with Testing Library

Source: https://svelte.dev/docs/svelte/testing

Example of rendering a component, querying elements, and simulating user interactions.

```javascript
test("Component", async () => {
  const user = userEvent.setup();
  render(Component);
  const button = screen.getByRole("button");
  expect(button).toHaveTextContent(0);
  await user.click(button);
  expect(button).toHaveTextContent(1);
});
```

---

### Prefers Reduced Motion Example

Source: https://svelte.dev/docs/svelte/svelte-motion

Demonstrates how to use the `prefersReducedMotion` media query to conditionally apply transitions. If reduced motion is preferred, a transition is disabled.

```html
<script>
	import { prefersReducedMotion } from 'svelte/motion';
	import { fly } from 'svelte/transition';

	let visible = $state(false);
</script>

<button onclick={() => visible = !visible}>
	toggle
</button>

{#if visible}
	<p transition:fly={{ y: prefersReducedMotion.current ? 0 : 200 }}>
		flies in, unless the user prefers reduced motion
	</p>
{/if}
```

---

### Using the fork API for Preloading in Svelte

Source: https://svelte.dev/docs/svelte/await-expressions/llms.txt

This example demonstrates how to use the `fork` API to preload content when a user shows intent to navigate. It manages a pending fork and commits or discards it based on user interaction.

```svelte
<script>
	import { fork } from 'svelte';
	import Menu from './Menu.svelte';

	let open = $state(false);

	/** @type {import('svelte').Fork | null} */
	let pending = null;

	function preload() {
		pending ??= fork(() => {
			open = true;
		});
	}

	function discard() {
		pending?.discard();
		pending = null;
	}
</script>

<button
	onfocusin={preload}
	onfocusout={discard}
	onpointerenter={preload}
	onpointerleave={discard}
	onclick={() => {
		pending?.commit();
		pending = null;

		// in case `pending` didn't exist
		// (if it did, this is a no-op)
		open = true;
	}}
>open menu</button>

{#if open}
	<!-- any async work inside this component will start
	     as soon as the fork is created -->
	<Menu onclose={() => open = false} />
{/if}

```

---

### createContext

Source: https://svelte.dev/docs/svelte/svelte

Returns a get and set pair of functions for working with context in a type-safe way. The get function will throw an error if no parent component called set.

```APIDOC
## createContext<T>()

### Description
Returns a [get, set] pair of functions for working with context in a type-safe way. get will throw an error if no parent component called set.

### Returns
- **[() => T, (context: T) => T]** - A tuple containing the getter and setter functions.
```

---

### Render a chess board

Source: https://svelte.dev/docs/svelte/each/llms.txt

Example of using nested each blocks without items to render a grid.

```svelte
<div class="chess-board">
	{#each { length: 8 }, rank}
		{#each { length: 8 }, file}
			<div class:black={(rank + file) % 2 === 1}></div>
		{/each}
	{/each}
</div>

<style>
	.chess-board {
		display: grid;
		grid-template-columns: repeat(8, 1fr);
		grid-template-rows: repeat(8, 1fr);
		border: 1px solid black;
		aspect-ratio: 1;

		.black {
			background: black;
		}
	}
</style>
```

---

### Node.js Console: Console Class Examples

Source: https://svelte.dev/docs/svelte/stores

Shows how to create a custom Console instance for directing logs to specific streams (stdout and stderr). Useful for more controlled logging in Node.js.

```javascript
const out = getStreamSomehow();
const err = getStreamSomehow();
const myConsole = new console.Console(out, err);

myConsole.log("hello world");
// Prints: hello world, to out
myConsole.log("hello %s", "world");
// Prints: hello world, to out
myConsole.error(new Error("Whoops, something bad happened"));
// Prints: [Error: Whoops, something bad happened], to err

const name = "Will Robinson";
myConsole.warn(`Danger ${name}! Danger!`);
// Prints: Danger Will Robinson! Danger!, to err
```

---

### Svelte Attachment Setup Function

Source: https://svelte.dev/docs/svelte/%40attach

Defines a Svelte attachment function that performs setup and update operations. It takes a 'bar' parameter and returns a function to be applied to a DOM node.

```typescript
function foo(bar: any): (node: any) => void {
  return (node) => {
    veryExpensiveSetupWork(node);
    update(node, bar);
  };
}
```

---

### Display await result in markup

Source: https://svelte.dev/docs/svelte/await-expressions

Example of how the UI reflects the result of an awaited expression.

```html
<p>2 + 2 = 3</p>
```

---

### Run Svelte code diagnostics

Source: https://svelte.dev/docs/svelte/getting-started/llms.txt

Use the Svelte CLI to check your project code for issues directly from the command line.

```shell
npx sv check
```

---

### Setting CSP Header with Nonce

Source: https://svelte.dev/docs/svelte/hydratable

Example of setting the Content-Security-Policy header using the generated nonce.

```javascript
headers.set("Content-Security-Policy", `script-src 'nonce-${nonce}'`);
```

---

### Demonstrating redundant data fetching during hydration

Source: https://svelte.dev/docs/svelte/hydratable

This example shows the pitfall where asynchronous data is re-fetched on the client, blocking hydration.

```svelte
<script>
  import { getUser } from 'my-database-library';

  // This will get the user on the server, render the user's name into the h1,
  // and then, during hydration on the client, it will get the user _again_,
  // blocking hydration until it's done.
  const user = await getUser();
</script>

<h1>{user.name}</h1>
```

---

### Access component property

Source: https://svelte.dev/docs/svelte/legacy-component-api

Example of accessing and incrementing a component property.

```javascript
log(component.count);
component.count += 1;
```

---

### Spring.of Static Method Example

Source: https://svelte.dev/docs/svelte/svelte-motion

Shows how to create a Spring instance that is bound to a reactive value using the static `of` method. This must be called within an effect root.

```html
<script>
  import { Spring } from "svelte/motion";

  let { number } = $props();

  const spring = Spring.of(() => number);
</script>
```

---

### Initialize a standalone Svelte project with Vite

Source: https://svelte.dev/docs/svelte/getting-started/llms.txt

An alternative method to set up Svelte using Vite directly. This approach requires manual configuration of routing and other features compared to SvelteKit.

```shell
npm create vite@latest
```

---

### createContext

Source: https://svelte.dev/docs/svelte/svelte

Returns a [get, set] pair of functions for working with context in a type-safe way.

```APIDOC
## createContext<T>()

### Description
Returns a [get, set] pair of functions for working with context in a type-safe way. get will throw an error if no parent component called set.

### Returns
- **[() => T, (context: T) => T]** - A pair of functions for getting and setting context.
```

---

### Node.js Console: Global Instance Examples

Source: https://svelte.dev/docs/svelte/stores

Demonstrates using the global console object for logging messages and errors to stdout and stderr. Useful for debugging in Node.js applications.

```javascript
console.log("hello world");
// Prints: hello world, to stdout
console.log("hello %s", "world");
// Prints: hello world, to stdout
console.error(new Error("Whoops, something bad happened"));
// Prints error message and stack trace to stderr:
//   Error: Whoops, something bad happened
//     at [eval]:5:15
//     at Script.runInThisContext (node:vm:132:18)
//     at Object.runInThisContext (node:vm:309:38)
//     at node:internal/process/execution:77:19
//     at [eval]-wrapper:6:22
//     at evalScript (node:internal/process/execution:76:60)
//     at node:internal/main/eval_string:23:3

const name = "Will Robinson";
console.warn(`Danger ${name}! Danger!`);
// Prints: Danger Will Robinson! Danger!, to stderr
```

---

### Derived Store with Multiple Stores and Cleanup

Source: https://svelte.dev/docs/svelte/stores

This example creates a derived store that uses `setInterval` to update a value based on a frequency store. It includes a cleanup function to clear the interval when the store is no longer subscribed.

```javascript
import { derived, writable } from "svelte/store";

const frequency = writable(1);

const intervalStore = derived(
  frequency,
  ($frequency, set) => {
    const interval = setInterval(() => {
      set(Date.now());
    }, 1000 / $frequency);

    return () => {
      clearInterval(interval);
    };
  },
  0,
);

// To use: subscribe to intervalStore
```

---

### Example of $state.raw with Array Reassignment

Source: https://svelte.dev/docs/svelte/%24state

Demonstrates updating state declared with $state.raw by reassigning the entire array, suitable for performance with large, non-mutated data.

```javascript
<script>
  let items = $state.raw([0]);

  const addItem = () => {
	items = [...items, items.length];
  };
</script>

<button onclick={addItem}>
  {items.join(', ')}
</button>
```

---

### Get DOM Node Reference

Source: https://svelte.dev/docs/svelte/bind

Use bind:this to get a direct reference to a DOM node. Read the reference inside an effect or event handler, as it's undefined during initialisation.

```html
bind:this={dom_node}
```

```javascript
let canvas;

$effect(() => {
  const ctx = canvas.getContext("2d");
  drawStuff(ctx);
});
```

```html
<canvas bind:this="{canvas}"></canvas>
```

---

### Interact with Component Instances

Source: https://svelte.dev/docs/svelte/bind

Use bind:this to get a reference to a component instance, allowing programmatic interaction. Ensure instance exports are available.

```html
<ShoppingCart bind:this="{cart}" />

<button onclick="{()" ="">cart.empty()}> Empty shopping cart</button>
```

```javascript
export function empty() {
  // ...
}
```

---

### Initialize a component with props

Source: https://svelte.dev/docs/svelte/legacy-component-api

Instantiate a component by providing a target element and initial properties.

````javascript
import ```
type App = SvelteComponent<Record<string, any>, any, any>
const App: LegacyComponentType
````

`App from './App.svelte'; const `const app: SvelteComponent<Record<string, any>, any, any>`app = new `new App(o: ComponentConstructorOptions): SvelteComponent`App({ `ComponentConstructorOptions<Record<string, any>>.target: Document | Element | ShadowRoot`target: `var document: Document`
**`window.document`**returns a reference to the document contained in the window.
MDN Reference
document.`Document.body: HTMLElement`
The **`Document.body`**property represents the or node of the current document, or null if no such element exists.
MDN Reference
body, `ComponentConstructorOptions<Record<string, any>>.props?: Record<string, any> | undefined`props: { // assuming App.svelte contains something like // `export let answer`: `answer: number`answer: 42 } });`

````

--------------------------------

### Implement MediaQuery with createSubscriber

Source: https://svelte.dev/docs/svelte/svelte-reactivity

Integrates window.matchMedia with Svelte's reactivity system to trigger updates when media query states change.

```javascript
import { createSubscriber } from 'svelte/reactivity';
import { on } from 'svelte/events';

export class MediaQuery {
	#query;
	#subscribe;

	constructor(query) {
		this.#query = window.matchMedia(`(${query})`);

		this.#subscribe = createSubscriber((update) => {
			// when the `change` event occurs, re-run any effects that read `this.current`
			const off = on(this.#query, 'change', update);

			// stop listening when all the effects are destroyed
			return () => off();
		});
	}

	get current() {
		// This makes the getter reactive, if read in an effect
		this.#subscribe();

		// Return the current state of the query, whether or not we're in an effect
		return this.#query.matches;
	}
}
````

---

### Get Current Store Value in Svelte

Source: https://svelte.dev/docs/svelte/stores

The `get` utility allows you to retrieve the current value of a Svelte store without subscribing. Use this sparingly as it involves creating and immediately destroying a subscription.

```typescript
import { get, writable } from "svelte/store";

const store = writable<string>("initial value");
const value = get(store);

console.log(value); // 'initial value'
```

---

### Asynchronous Server-Side Rendering

Source: https://svelte.dev/docs/svelte/await-expressions/llms.txt

Demonstrates how to perform asynchronous server-side rendering using `render(...)` from `svelte/server`. Await the result of `render` to get the head and body HTML. Frameworks like SvelteKit handle this automatically.

```javascript
import { render } from "svelte/server";
import App from "./App.svelte";

const { head, body } = await render(App);
```

---

### Basic Context with createContext

Source: https://svelte.dev/docs/svelte/context/llms.txt

Demonstrates setting and getting user context in Svelte components using `createContext`. This is useful when parent and child components are not directly aware of each other.

```svelte
<!--- file: App.svelte --->
<script>
	import Parent from './Parent.svelte';
	import Child from './Child.svelte';
</script>

<Parent>
	<Child />
</Parent>
```

```svelte
<!--- file: Parent.svelte --->
<script>
	import { setUserContext } from './context';

	let { children } = $props();

	setUserContext({ name: 'world' });
</script>

{@render children()}
```

```svelte
<!--- file: Child.svelte --->
<script>
	import { getUserContext } from './context';

	const user = getUserContext();
</script>

<h1>hello {user.name}, inside Child.svelte</h1>
```

```ts
/// file: context.ts
import { createContext } from "svelte";

interface User {
  name: string;
}

export const [getUserContext, setUserContext] = createContext<User>();
```

---

### Get DOM Node Reference with bind:this

Source: https://svelte.dev/docs/svelte/bind/llms.txt

Use `bind:this` to get a reference to a DOM node. The value is `undefined` until the component is mounted, so read it in effects or event handlers.

```svelte
<!--- copy: false --->
bind:this={dom_node}
```

```svelte
<script>
	/** @type {HTMLCanvasElement} */
	let canvas;

	$effect(() => {
		const ctx = canvas.getContext('2d');
		drawStuff(ctx);
	});
</script>

<canvas bind:this={canvas}></canvas>
```

---

### Basic Derived Store Example

Source: https://svelte.dev/docs/svelte/stores

This snippet shows a basic derived store that increments a value after a delay. It demonstrates the use of `setTimeout` within the derived store's callback.

```javascript
import { derived, writable } from "svelte/store";

const count = writable(0);

const delayedCount = derived(count, ($count) => {
  setTimeout(() => {
    // This will be called when count changes
    console.log($count + 1);
  }, 1000);
  return $count;
});
```

---

### Basic Tween Initialization

Source: https://svelte.dev/docs/svelte/svelte-motion

Initializes a Tween store with a starting value. Use this to create a value that can be smoothly animated towards a target.

```html
<script>
  import { Tween } from "svelte/motion";

  const tween = new Tween(0);
</script>

<input type="range" bind:value="{tween.target}" />
<input type="range" bind:value="{tween.current}" disabled />
```

---

### Prefers Reduced Motion Example

Source: https://svelte.dev/docs/svelte/svelte-motion

Uses the `prefersReducedMotion` media query to conditionally apply transitions. If the user prefers reduced motion, a simpler transition (or none) is used.

```html
<script>
	import { prefersReducedMotion } from 'svelte/motion';
	import { fly } from 'svelte/transition';

	let visible = $state(false);
</script>

<button onclick={() => visible = !visible}>
	toggle
</button>

{#if visible}
	<p transition:fly={{ y: prefersReducedMotion.current ? 0 : 200 }}>
		flies in, unless the user prefers reduced motion
	</p>
{/if}

```

---

### Initialize a component with hydration

Source: https://svelte.dev/docs/svelte/legacy-component-api

Instantiate a component using the hydrate option to upgrade existing server-rendered DOM.

````javascript
import ```
type App = SvelteComponent<Record<string, any>, any, any>
const App: LegacyComponentType
````

`App from './App.svelte'; const `const app: SvelteComponent<Record<string, any>, any, any>`app = new `new App(o: ComponentConstructorOptions): SvelteComponent`App({ `ComponentConstructorOptions<Record<string, any>>.target: Document | Element | ShadowRoot`target: `var document: Document`
**`window.document`**returns a reference to the document contained in the window.
MDN Reference
document.`ParentNode.querySelector<Element>(selectors: string): Element | null (+4 overloads)`Returns the first element that is a descendant of node that matches selectors.
MDN Reference
querySelector('#server-rendered-html'),`ComponentConstructorOptions<Record<string, any>>.hydrate?: boolean | undefined`hydrate: true });`

````

--------------------------------

### Component Stories with Storybook

Source: https://svelte.dev/docs/svelte/testing/llms.txt

Create stories for different component variations and test interactions using Storybook's play function. This example demonstrates testing an empty and a filled login form.

```svelte
/// file: LoginForm.stories.svelte
<script module>
	import { defineMeta } from '@storybook/addon-svelte-csf';
	import { expect, fn } from 'storybook/test';

	import LoginForm from './LoginForm.svelte';

	const { Story } = defineMeta({
		component: LoginForm,
		args: {
			// Pass a mock function to the `onSubmit` prop
			onSubmit: fn(),
		}
	});
</script>

<Story name="Empty Form" />

<Story
	name="Filled Form"
	play={async ({ args, canvas, userEvent }) => {
		// Simulate a user filling out the form
		await userEvent.type(canvas.getByTestId('email'), 'email@provider.com');
		await userEvent.type(canvas.getByTestId('password'), 'a-random-password');
		await userEvent.click(canvas.getByRole('button'));

		// Run assertions
		await expect(args.onSubmit).toHaveBeenCalledTimes(1);
		await expect(canvas.getByText('You’re in!')).toBeInTheDocument();
	}}
/>
````

---

### Svelte get Function

Source: https://svelte.dev/docs/svelte/svelte-store/llms.txt

The 'get' function in TypeScript retrieves the current value of a Svelte store. It achieves this by subscribing to the store and immediately unsubscribing after obtaining the value, ensuring no persistent subscription is created.

```typescript
function get<T>(store: Readable<T>): T;
```

---

### mount(component, options)

Source: https://svelte.dev/docs/svelte/imperative-component-api

Instantiates a component and mounts it to the specified target element. It returns the component exports and props.

```APIDOC
## mount(component, options)

### Description
Instantiates a component and mounts it to the given target. Transitions will play during the initial render unless the intro option is set to false.

### Parameters
- **component** (ComponentType | Component) - Required - The Svelte component to mount.
- **options** (MountOptions) - Required - Configuration object including the target element and component props.

### Returns
- **Exports** - The exports and potentially the props of the component.
```

---

### Get Current Value of Svelte Store

Source: https://svelte.dev/docs/svelte/stores/llms.txt

The `get` function allows retrieving the current value of a store without subscribing to it. This is useful in situations where direct subscription is not feasible, but it's not recommended for performance-critical code paths as it involves a temporary subscription.

```typescript
// @filename: ambient.d.ts
import { type Writable } from "svelte/store";

declare global {
  const store: Writable<string>;
}

export {};

// @filename: index.ts
// ---cut---
import { get } from "svelte/store";

const value = get(store);
```

---

### Svelte toStore Function

Source: https://svelte.dev/docs/svelte/svelte-store/llms.txt

The 'toStore' function in TypeScript converts a getter/setter pair or a getter into a Svelte store. It can create a Writable store if both get and set functions are provided, or a Readable store if only a get function is supplied.

```typescript
function toStore<V>(get: () => V, set: (v: V) => void): Writable<V>;
```

```typescript
function toStore<V>(get: () => V): Readable<V>;
```

---

### get

Source: https://svelte.dev/docs/svelte/svelte-store

Retrieves the current value from a store by subscribing and immediately unsubscribing.

```APIDOC
## get

### Description
Get the current value from a store by subscribing and immediately unsubscribing.

### Method
Function

### Parameters
- **store** (Readable<T>) - Required - The store to read from.

### Response
- **T** - The current value of the store.
```

---

### Svelte 4 Event Listener Syntax

Source: https://svelte.dev/docs/svelte/v5-migration-guide

Example of the deprecated 'on:' directive used in Svelte 4.

```svelte
<script>
	let count = $state(0);
</script>

<button on:click={() => count++}>
	clicks: {count}
</button>
```

---

### Defining a Typed Action

Source: https://svelte.dev/docs/svelte/svelte-action

Example of an action restricted to HTMLDivElement that accepts an optional parameter.

````typescript
export const ```
const myAction: Action<HTMLDivElement, {
    someProperty: boolean;
} | undefined>
````

`myAction: `type Action = /_unresolved_/ any`Action<HTMLDivElement, { `someProperty: boolean`someProperty: boolean } | undefined> = (`node: any`node, ````
param: {
someProperty: boolean;
}

```
`param = { `someProperty: boolean`someProperty: true }) => { // ... }
```

---

### Create a Writable Store in Svelte

Source: https://svelte.dev/docs/svelte/stores

Initializes a writable store with an optional starting value and a start/stop notifier function. Use this for managing state that can be updated and subscribed to.

```typescript
import { writable } from "svelte/store";

const count: Writable<number> = writable<number>(0, () => {
  console.log("got a subscriber");
  return () => {
    console.log("no subscribers left");
  };
});
```

---

### Write Playwright E2E test

Source: https://svelte.dev/docs/svelte/testing

Example of a basic test file interacting with the DOM using Playwright.

```javascript
import { expect, test } from "@playwright/test";

test("home page has expected h1", async ({ page }) => {
  await page.goto("/");
  await expect(page.locator("h1")).toBeVisible();
});
```

---

### Create a component instance

Source: https://svelte.dev/docs/svelte/legacy-component-api

Basic syntax for instantiating a Svelte component class.

```javascript
const const component: anycomponent = new Component(options);
```

---

### Generating stable random values with hydratable

Source: https://svelte.dev/docs/svelte/hydratable

This example demonstrates using hydratable to ensure random or time-based values remain consistent between server rendering and hydration.

```javascript
import { function hydratable<T>(key: string, fn: () => T): T
reference
hydratable } from 'svelte';
const const rand: numberrand = hydratable<number>(key: string, fn: () => number): number
reference
hydratable('random', () => var Math: Math
An intrinsic object that provides basic mathematics functionality and constants.


Math.Math.random(): number
Returns a pseudorandom number between 0 and 1.


random());
```

---

### Reactivity Loss Warning

Source: https://svelte.dev/docs/svelte/runtime-warnings

Example of a warning message triggered by reactivity loss in async functions.

```text
Detected reactivity loss when reading `%name%`. This happens when state is read in an async function after an earlier `await`
```

---

### Stale Assignment Example

Source: https://svelte.dev/docs/svelte/runtime-warnings

A code pattern that causes the stale assignment warning.

```svelte
<script>
	let object = $state({ array: null });

	function add() {
		(object.array ??= []).push(object.array.length);
	}
</script>

<button onclick={add}>add</button>
<p>items: {JSON.stringify(object.items)}</p>
```

---

### Svelte Component Example (App.svelte)

Source: https://svelte.dev/docs/svelte/overview/llms.txt

A basic Svelte component demonstrating a button that triggers a JavaScript alert when clicked. It includes script, button, and style sections within a single .svelte file.

```svelte
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Svelte App</title>
</head>
<body>
    <script src="./App.js"></script>
</body>
</html>
```

```svelte
<!--- file: App.svelte --->
<script>
	function greet() {
		alert('Welcome to Svelte!');
	}
</script>

<button onclick={greet}>click me</button>

<style>
	button {
		font-size: 2em;
	}
</style>
```

---

### mount(component, options)

Source: https://svelte.dev/docs/svelte/testing

Instantiates a Svelte component for testing purposes.

```APIDOC
## mount(component, options)

### Description
Instantiates the component using Svelte's mount API for testing.

### Parameters
- **component** (ComponentType) - Required - The Svelte component to mount.
- **options** (MountOptions) - Required - Configuration options for mounting the component.
```

---

### Get AbortSignal

Source: https://svelte.dev/docs/svelte/svelte

Retrieves the current AbortSignal.

```typescript
function getAbortSignal(): AbortSignal;
```

---

### Console Class Warn Example

Source: https://svelte.dev/docs/svelte/v4-migration-guide

Illustrates warning messages to custom streams using the Console class. Warnings are sent to the specified error stream.

```javascript
const out = getStreamSomehow();
const err = getStreamSomehow();
const myConsole = new console.Console(out, err);

const name = "Will Robinson";
myConsole.warn(`Danger ${name}! Danger!`);
// Prints: Danger Will Robinson! Danger!, to err
```

---

### Instantiate Svelte 5 Components with `mount` or `hydrate`

Source: https://svelte.dev/docs/svelte/compiler-warnings

Svelte 5 components are no longer classes. Instantiate them using `mount` or `hydrate`, imported from 'svelte', instead of traditional class instantiation.

```javascript
import { mount } from "svelte";
// or
import { hydrate } from "svelte";
```

---

### Svelte 4 Event Modifiers

Source: https://svelte.dev/docs/svelte/v5-migration-guide

Example of Svelte 4 event modifiers syntax.

```svelte
<button on:click|once|preventDefault={handler}>...</button>
```

---

### Basic Reactive Statements and Assignments

Source: https://svelte.dev/docs/svelte/legacy-reactive-assignments

Demonstrates using $: for logging and reactive variable assignment based on other state variables.

```svelte
<script>
	let a = 1;
	let b = 2;

	// this is a 'reactive statement', and it will re-run
	// when `a`, `b` or `sum` change
	$: console.log(`${a} + ${b} = ${sum}`);

	// this is a 'reactive assignment' — `sum` will be
	// recalculated when `a` or `b` change. It is
	// not necessary to declare `sum` separately
	$: sum = a + b;
</script>
```

---

### Spring Class Example

Source: https://svelte.dev/docs/svelte/svelte-motion

Demonstrates the usage of the Spring class to animate a value. It uses input range sliders to control the target and display the current value.

```html
<script>
  import { Spring } from "svelte/motion";

  const spring = new Spring(0);
</script>

<input type="range" bind:value="{spring.target}" />
<input type="range" bind:value="{spring.current}" disabled />
```

---

### Svelte 5 Property-based Event Handler

Source: https://svelte.dev/docs/svelte/v5-migration-guide

Example of using standard property syntax for event handlers in Svelte 5.

```svelte
<script>
	let count = $state(0);

	function onclick() {
		count++;
	}
</script>

<button {onclick}>
	clicks: {count}
</button>
```

---

### Legacy component instantiation

Source: https://svelte.dev/docs/svelte/v5-migration-guide

The old way of mounting components using the new keyword and $destroy method.

```javascript
const app = new App({
  target: document.getElementById("app"),
  props: { foo: "bar" },
});
app.$destroy();
```

---

### Svelte Component Structure Example

Source: https://svelte.dev/docs/svelte/svelte-files/llms.txt

Demonstrates the basic structure of a Svelte component file (.svelte), including optional script, style, and markup sections. The `<script module>` block is for module-level logic, while the main `<script>` block handles instance-level logic.

```svelte
/// file: MyComponent.svelte
<script module>
	// module-level logic goes here
	// (you will rarely use this)
</script>

<script>
	// instance-level logic goes here
</script>

<!-- markup (zero or more items) goes here -->

<style>
	/* styles go here */
</style>
```

---

### setContext After Initialization

Source: https://svelte.dev/docs/svelte/runtime-errors

The `setContext` function must be called during the initial component setup. It cannot be used in subsequent effects or after an `await` expression, especially when `experimental.async` is enabled.

```text
`setContext` must be called when a component first initializes, not in a subsequent effect or after an `await` expression
```

---

### Log a message with substitution to the console

Source: https://svelte.dev/docs/svelte/%24state

Shows how to use substitution values (e.g., %s) in console.log, similar to printf.

```javascript
console.log("hello %s", "world");
```

---

### Define Storybook stories for testing

Source: https://svelte.dev/docs/svelte/testing

Example of using Storybook to define component stories with mock functions for testing interactions.

```svelte
<script module>
	import { defineMeta } from '@storybook/addon-svelte-csf';
	import { expect, fn } from 'storybook/test';

	import LoginForm from './LoginForm.svelte';

	const { Story } = defineMeta({
		component: LoginForm,
		args: {
			// Pass a mock function to the `onSubmit` prop
			onSubmit: fn(),
		}
	});
</script>

<Story name="Empty Form" />
```

---

### TweenOptions 'delay' Property

Source: https://svelte.dev/docs/svelte/svelte-motion

The delay in milliseconds before the tween animation starts.

```typescript
delay?: number;

```

---

### Legacy Component Creation

Source: https://svelte.dev/docs/svelte/compiler-warnings/llms.txt

Svelte 5 components are no longer class-based. Instantiate them using `mount` or `hydrate` imported from 'svelte'. Refer to the migration guide for details.

```svelte
Svelte 5 components are no longer classes. Instantiate them using `mount` or `hydrate` (imported from 'svelte') instead.
```

---

### Getting Context with getContext

Source: https://svelte.dev/docs/svelte/context/llms.txt

A child component retrieves context using `getContext` with the corresponding key.

```svelte
<!--- file: Child.svelte --->
<script>
	import { getContext } from 'svelte';

	const message = getContext('my-context');
</script>

<h1>{message}, inside Child.svelte</h1>
```

---

### get Utility

Source: https://svelte.dev/docs/svelte-store

Retrieves the current value of a store by subscribing and immediately unsubscribing.

````APIDOC
## get

Get the current value from a store by subscribing and immediately unsubscribing.

### Method Signature

```javascript
function get<T>(store: Readable<T>): T;
````

````

--------------------------------

### App component with unbound props

Source: https://svelte.dev/docs/svelte/runtime-warnings

Example of an App component passing a state object to a child component.

```svelte
<script>
	import Child from './Child.svelte';
	let person = $state({ name: 'Florida', surname: 'Man' });
</script>

<Child {person} />
````

```typescript
<script lang="ts">
	import Child from './Child.svelte';
	let person = $state({ name: 'Florida', surname: 'Man' });
</script>

<Child {person} />
```

---

### Stale Assignment Value Warning

Source: https://svelte.dev/docs/svelte/runtime-warnings

Example of a warning message triggered by stale property assignment.

```text
Assignment to `%property%` property (%location%) will evaluate to the right-hand side, not the value of `%property%` following the assignment. This may result in unexpected behaviour.
```

---

### Derived Store with Multiple Dependencies

Source: https://svelte.dev/docs/svelte/stores

Shows how to create a derived store that depends on multiple other stores. The derived store updates whenever any of its dependent stores change.

```javascript
import { writable, derived } from "svelte/store";

const count = writable(0);
const multiplier = writable(2);

const product = derived([count, multiplier], ([$count, $multiplier]) => {
  return $count * $multiplier;
});

product.subscribe((value) => {
  console.log(value);
});

count.set(3);
// console.log will output 6
multiplier.set(4);
// console.log will output 12
```

---

### Use the Console class

Source: https://svelte.dev/docs/svelte/custom-elements

Instantiate a custom Console object to write to specific streams.

```javascript
const out = getStreamSomehow();
const err = getStreamSomehow();
const myConsole = new console.Console(out, err);

myConsole.log("hello world");
// Prints: hello world, to out
myConsole.log("hello %s", "world");
// Prints: hello world, to out
myConsole.error(new Error("Whoops, something bad happened"));
// Prints: [Error: Whoops, something bad happened], to err
```

---

### Get an AbortSignal

Source: https://svelte.dev/docs/svelte/svelte

Retrieve an AbortSignal that triggers when the current effect or derived state re-runs or is destroyed.

```svelte
<script>
	import { getAbortSignal } from 'svelte';

	let { id } = $props();

	async function getData(id) {
		const response = await fetch(`/items/${id}`, {
			signal: getAbortSignal()
		});

		return await response.json();
	}

	const data = $derived(await getData(id));
</script>
```

---

### Console Class Error Example

Source: https://svelte.dev/docs/svelte/v4-migration-guide

Shows error logging to custom streams with the Console class. Errors are directed to the specified error stream.

```javascript
const out = getStreamSomehow();
const err = getStreamSomehow();
const myConsole = new console.Console(out, err);

myConsole.error(new Error("Whoops, something bad happened"));
// Prints: [Error: Whoops, something bad happened], to err
```

---

### Tween Class Example

Source: https://svelte.dev/docs/svelte/svelte-motion

Illustrates the use of the Tween class for smooth, time-based transitions. Similar to Spring, it uses input sliders for target and current values.

```html
<script>
  import { Tween } from "svelte/motion";

  const tween = new Tween(0);
</script>

<input type="range" bind:value="{tween.target}" />
<input type="range" bind:value="{tween.current}" disabled />
```

---

### Export Snippets from a Module Script

Source: https://svelte.dev/docs/svelte/snippet

This example demonstrates exporting a snippet named 'add' from a `<script module>`. This allows the snippet to be imported and used in other components. Requires Svelte 5.5.0 or newer.

```svelte
<script>
	import { add } from './snippets.svelte';
</script>

{@render add(1, 2)}
```

```svelte
<script lang="ts">
	import { add } from './snippets.svelte';
</script>

{@render add(1, 2)}
```

```svelte
<script module>
	export { add };
</script>

{#snippet add(a, b)}
	{a} + {b} = {a + b}
{/snippet}
```

---

### Get Svelte Compiler Version

Source: https://svelte.dev/docs/svelte-compiler

Retrieves the current version of the Svelte compiler, as defined in package.json.

```typescript
const VERSION: string;
```

---

### Create a reactive media query

Source: https://svelte.dev/docs/svelte/svelte-reactivity

Use MediaQuery to track screen size changes. Avoid using this during server-side rendering as it may cause hydration mismatches.

```svelte
<script>
	import { MediaQuery } from 'svelte/reactivity';

	const large = new MediaQuery('min-width: 800px');
</script>

<h1>{large.current ? 'large screen' : 'small screen'}</h1>
```

---

### Get all contexts

Source: https://svelte.dev/docs/svelte/svelte

Retrieves the entire context map from the closest parent component. Must be called during component initialization.

```typescript
function getAllContexts<T extends Map<any, any> = Map<any, any>>(): T;
```

---

### Log a simple message using the Console class

Source: https://svelte.dev/docs/svelte/%24state

Creates a Console instance and logs a simple string message to its configured output stream.

```javascript
const out = getStreamSomehow();
const err = getStreamSomehow();
const myConsole = new console.Console(out, err);

myConsole.log("hello world");
```

---

### Configure custom element options

Source: https://svelte.dev/docs/svelte/custom-elements

Demonstrates how to define custom element tags, shadow DOM settings, property attributes, and class extension for form participation.

```svelte
<svelte:options
	customElement={{
		tag: 'custom-element',
		shadow: {
			mode: import.meta.env.DEV ? 'open' : 'closed',
			clonable: true,
			// ...
		},
		props: {
			name: { reflect: true, type: 'Number', attribute: 'element-index' }
		},
		extend: (customElementConstructor) => {
			// Extend the class so we can let it participate in HTML forms
			return class extends customElementConstructor {
				static formAssociated = true;

				constructor() {
					super();
					this.attachedInternals = this.attachInternals();
				}

				// Add the function here, not below in the component so that
				// it's always available, not just when the inner Svelte component
				// is mounted
				randomIndex() {
					this.elementIndex = Math.random();
				}
			};
		}
	}}
/>

<script>
	let { elementIndex, attachedInternals } = $props();
	// ...
	function check() {
		attachedInternals.checkValidity();
	}
</script>

...
```

---

### Logging with Node.js Console

Source: https://svelte.dev/docs/svelte/svelte-boundary

Provides examples of using the global console object and the Console class to output logs, warnings, and errors to standard streams.

```javascript
console.log("hello world");
console.error(new Error("Whoops, something bad happened"));

const myConsole = new console.Console(process.stdout, process.stderr);
myConsole.warn("Danger!");
```

---

### Spring configuration options

Source: https://svelte.dev/docs/svelte/svelte-motion/llms.txt

Configuration interface for initializing a Spring instance.

```typescript
interface SpringOptions {
  /*…*/
}
```

```typescript
stiffness?: number;
```

```typescript
damping?: number;
```

```typescript
precision?: number;
```
